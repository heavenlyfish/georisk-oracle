# GeoRisk Oracle dependencies
openai>=1.0.0              # NIM uses OpenAI-compatible API
python-dotenv>=1.0.0       # .env loading
requests>=2.31.0           # HTTP (NewsAPI, Telegram)
pydantic>=2.0.0            # Typed schema for RiskAssessment
pydantic-settings>=2.0.0   # Settings from env vars
rich>=13.0.0               # CLI formatting / pretty output
nemoguardrails>=0.10.0    # NeMo Guardrails policy layer (NemoClaw bonus ⭐)
